# Minimal IoTDB system config for local runs
# Only override the TsBlock row limit here; others use defaults.

# The max number of lines in a single TsBlock
#max_tsblock_line_number=5

# Optionally raise byte cap so rows, not bytes, cut the block

query_timeout_threshold =3600000

# Disable read-side caches to make repeated queries deterministic
meta_data_cache_enable=false
chunk_timeseriesmeta_free_memory_proportion=0:0:0:50:200:200:200:50
enable_last_cache=false
device_path_cache_proportion=0
partition_cache_size=0
remote_schema_cache_size=0
